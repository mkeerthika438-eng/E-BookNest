PORT=5000

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Library@890
DB_NAME=ebooknest
DB_PORT=3306

JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d

MAX_RENEWALS=2
BORROW_DAYS=14
RENEWAL_DAYS=7
FINE_PER_DAY=5
DUE_SOON_DAYS=3

CLIENT_URL=http://localhost:5173

# Email reminders (optional) — leave blank to disable email; in-app notifications
# will still work either way. For Gmail: use smtp.gmail.com, port 587, and a
# 16-character "App Password" (not your normal Gmail password) for SMTP_PASS.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
